# 安全策略

## 核心威胁模型

平台处理的 ZIP、SVN 内容、`pom.xml`、Maven 插件和构建脚本均视为不可信输入。即使使用 `-DskipTests`，Maven 生命周期中的插件仍可能执行代码。

## 必须遵守的边界

- Web/API 进程不得直接执行用户项目构建。
- 构建和扫描任务必须运行在一次性隔离环境中。
- Runner 不挂载宿主 Docker Socket、用户主目录或生产凭据。
- 默认禁止外网，只允许按策略访问内部 Maven 镜像和必要漏洞数据库。
- 限制 CPU、内存、磁盘、进程数量、执行时间和解压后总大小。
- ZIP 解压必须防目录穿越、符号链接逃逸和压缩炸弹。
- SVN URL 必须限制协议、目标网段和重定向，防止 SSRF。
- Secret 报告必须脱敏；日志和 API 不得保存完整密钥。
- 扫描完成后销毁源码工作区，不跨任务复用可写缓存。

## 报告安全问题

当前仓库处于初始化阶段。如发现安全问题，请不要建立公开 Issue；请通过 GitHub 私密漏洞报告功能联系维护者。
