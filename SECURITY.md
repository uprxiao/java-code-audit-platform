# 安全策略

完整威胁模型和实现要求见[V1安全边界](docs/v1/security-boundary.md)。

## 核心边界

V1 在宿主机直接运行 Maven 和扫描器，不提供容器或恶意代码沙箱。即使使用 `-DskipTests`，Maven插件和构建生命周期仍可能执行代码。因此：

- 只扫描维护者信任且有权分析的Java 17 Maven项目；
- 只部署在个人机器、VPN或可信内网；
- V1无认证，不能直接暴露为任意公网匿名上传服务；
- 使用低权限专用账户，不向服务环境提供生产凭据；
- macOS ARM64和Ubuntu 22.04 x86_64都执行安全回归。

## 必须实现

- ZIP路径、symlink、压缩炸弹、文件数和容量防护；
- SVN临时凭据只驻留内存，证书验证默认开启；
- ProcessBuilder参数数组，不经过Shell；
- 工作/输出/删除路径限制在任务根；
- 超时、取消和完整进程树清理；
- 工具版本、SHA256和许可清单；
- 统一密钥脱敏覆盖HTML、JSON、SARIF、raw和logs；
- 有界并发、磁盘水位和自动保留清理；
- 报告显示失败和未覆盖，不把失败表达为0问题。

## 报告安全问题

如发现安全问题，请不要建立公开Issue；请使用GitHub私密漏洞报告功能联系维护者。
