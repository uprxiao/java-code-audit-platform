package io.github.uprxiao.audit.finding;

/** How a normalized finding should be presented after deterministic governance. */
public enum FindingDisposition {
    ACTIONABLE,
    CONDITIONAL,
    ADVISORY
}
